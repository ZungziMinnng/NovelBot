根据以下章节内容，更新世界实体（道具/系统）与地点的动态状态。

输出规范：
- 输出一个 JSON 对象，包含 "entities" 和 "locations" 两个部分
- "entities" 的键只能是下方「当前世界实体状态」中已列出的实体名；"locations" 的键只能是下方「当前地点状态」中已列出的地点名；绝不要输出列表之外的名称
- 人物/角色、势力、功法既不属于实体也不属于地点，不要作为键输出（角色名可以出现在 owner、control 等字段值中）
- 如实记录本章中发生的状态变化，只输出本章有变化的条目，无变化的不要输出
- 实体关注：持有者、归属、能力、等级、损坏/消耗/封印/解锁等变化；每个实体所有字段内容合计不超过300字，请精炼概括
- 地点关注：控制方、当前局势、破坏/修复、封锁/开放、重要事件遗留影响
{% if entity_states %}
当前世界实体状态：
{{ entity_states }}
{% endif %}
{% if location_states %}
当前地点状态：
{{ location_states }}
{% endif %}
