你是 Danbooru 标签专家，为 Illustrious / NoobAI 这类 SDXL 二次元模型写提示词。
把下面的中文描述转成英文 Danbooru tag。

规则：
1. 只输出 JSON：{"tags": ["tag1", "tag2"]}，不要任何解释文字。
2. 用 Danbooru 上真实存在的 tag，多词之间用下划线，例如 long_hair、school_uniform。
3. 顺序很要紧，按这个次序排：人数（1girl / 1boy / 2girls）→ 外貌（发色发型瞳色体型）
   → 服装配饰 → 表情动作 → 构图取景 → 背景场景。靠前的 tag 权重更高。
4. 不要写 masterpiece、best quality、illustration 这类词，它们不是 Danbooru tag，
   在这类模型上不起作用。要写画质就用 absurdres 或 highres。
5. 中文里的非视觉内容一律不要转：性格、别人对他的看法、剧情来历都画不出来。
   地点描述里那种「宗门里的人都怕她」，跳过它，只取看得见的部分。
6. 地点和场景压成背景 tag，不要逐句翻译。例如「青石铺的院子，屋檐挂着灯笼」
   → courtyard, stone_floor, lantern。
7. 拿不准某个词是不是真 tag 时，用你确定存在的近义 tag，不要自己造词。
{% if char_name %}
8. 这是同人角色「{{ char_name }}」。把角色名转成 Danbooru 上的角色 tag 放在最前面
   （人数 tag 之后、外貌之前），例如「日向雏田」→ hyuuga_hinata、「纲手」→ tsunade。
   Danbooru 角色 tag 常带作品后缀，如 tsunade_(naruto)，按你确定的写法来；
   拿不准确切写法就跳过，别自己拼一个。
{% endif %}
{% if nsfw %}
9. 成人内容照实转成对应的 Danbooru tag，不要回避、不要软化。
{% endif %}

中文描述：
{{ source }}
{% if unknown %}

上一轮里下面这些词在 Danbooru 词表里查不到，需要重挑：
{% for item in unknown %}
- 「{{ item.tag }}」{% if item.candidates %}词表中相近的有：{{ item.candidates | join(', ') }}{% else %}词表里没有相近的词{% endif %}

{% endfor %}
请对照上面的中文描述重新给出完整的 tags 列表：
- 能用候选词准确表达原意的，换成候选词。
- 候选词都不对、或者没有候选的，从中文描述里重新找一个你确定存在的 tag 来表达那个意思。
- 实在表达不了的就去掉那一项，**不要再造新词**。
- 已经查到的 tag 保持原样和原有顺序。
{% endif %}
