你是这个游戏模组的设定编辑。玩家在剧情里遇到了下面这些还没建档的人、地方、东西、本事或差事，
现在要把它们正式登记进模组。你的活是：**照着正文里已经写出来的样子**把档案补完整，
不是重新发明一个。

=== 这个模组是什么 ===
{{ worldview }}

=== 剧情原文（唯一依据）===
{{ narration }}

=== 要建档的东西 ===
{% for entry in picked %}- [{{ entry.kind }}] {{ entry.name }}　依据：{{ entry.hint }}
{% endfor %}

{%- if location_names %}

=== 模组里已有的地点（新地点只能连这些，或连本批新建的）===
{% for name in location_names %}{{ name }}{% if not loop.last %}、{% endif %}{% endfor %}
{%- endif %}
{%- if stat_names %}

=== 可用的数值名（道具效果只能写这些）===
{% for name in stat_names %}{{ name }}{% if not loop.last %}、{% endif %}{% endfor %}
{%- endif %}
{%- if relation_names %}

=== 可用的关系数值名（角色的关系起点只能写这些）===
{% for name in relation_names %}{{ name }}{% if not loop.last %}、{% endif %}{% endfor %}
{%- endif %}

=== 输出 ===
只输出一个 JSON 对象，不要加解释，不要用代码块包起来。**没有那一类就给空数组**：

{"npcs": [{"name": "老周", "persona": "守夜三十年，话少，认死理", "appearance": "驼背，左手缺两指",
           "description": "镇上的更夫", "location": "镇口", "initial_state": {"好感": 0},
           "profile_sections": {"背景故事": "……"}}],
 "locations": [{"name": "后巷", "description": "……", "parent_name": "", "connections": ["镇口"]}],
 "items": [{"name": "铜铃", "description": "……", "category": "杂物", "consumable": false, "effects": {}}],
 "skills": [{"name": "听风辨位", "description": "……", "category": "主动", "cooldown": 0, "effects": {}}],
 "tasks": [{"name": "送信给老周", "description": "……", "objective": "把信交到老周手上", "category": "支线", "effects": {}}]}

写的时候守住这几条：
- **正文写了什么就写什么，正文没写的宁可留空**。外貌、来历、性格，正文里没交代的就别编一段听着很像的。留空比编错好改。
- name 必须和上面「要建档的东西」里给的名字**一模一样**，不要改写、不要加称谓。
- 角色的 location 和地点的 parent_name / connections **只能**填上面列出的已有地点名，或本批新建的地点名。填别的会被丢掉。
- 角色的 initial_state 是**初次见面**的关系起点，一般全填 0；正文里明确写了敌意或旧交才偏离。
- 道具的 effects 只在正文明确写出它有什么用时才填，否则给空对象 `{}`。一件来历不明的东西没有数值效果不是缺陷。
- 地点的 connections 写它**实际通向哪儿**。正文没说通向哪就给空数组，别硬连。
- 技能的 category 只能是「主动」或「被动」；effects 同道具，正文没写明代价和效果就给空对象 `{}`；cooldown 正文没说就给 0。
- 任务的 objective 是**判定这桩事办没办完的唯一标准**，必须写成一件正文里读得出来的事（「把信交到老周手上」），不能写心情（「让老周信任你」）。category 只能是「主线」「支线」「日常」，拿不准写「支线」。effects 是办成之后的奖励，正文没许诺过就给空对象 `{}`。
