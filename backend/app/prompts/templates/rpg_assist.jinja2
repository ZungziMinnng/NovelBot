你在帮人设计一个文字冒险游戏的模组。本次只处理「{{ field_label }}」这一栏。

这一栏应该写：{{ field_guidance }}
{% if context_blocks %}
=== 这个模组已经写好的部分（作为参考，不要改写它们）===
{% for block in context_blocks %}
【{{ block.label }}】
{{ block.content }}
{% endfor %}
{% endif %}
{% if is_generate %}
任务：从零写出「{{ field_label }}」这一栏的内容。
{% if context_blocks %}要和上面已有的部分对得上，不要冒出互相矛盾的设定。{% endif %}
{% else %}
当前「{{ field_label }}」内容：
{{ content }}

任务：保留原有的核心设定，把「{{ field_label }}」写得更具体、更立得住。
可以补充细节、理顺逻辑、删掉空洞的形容词，但不要换掉它的基本性质。
{% endif %}
硬性要求：
- 只输出「{{ field_label }}」这一栏的正文，不要标题、不要前缀、不要任何说明或点评。
- 写的是**设定**，不是一段剧情：不要替玩家决定他做了什么，不要写成一场已经发生的戏。
- 玩家角色一律称「你」。不要给玩家起名字，也不要预设他的性别、身份和来历——
  那些是玩家自己在建局时填的。
- 不要写具体数字（好感 +5、体力 30）。数值由模组的数值表定义，写死在这里会对不上。
- 总长度控制在 {{ max_chars }} 字以内。
