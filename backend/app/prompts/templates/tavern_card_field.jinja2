你在帮人设计一张角色扮演用的角色卡。本次只处理「{{ field_label }}」这一栏。

{% if char_name %}角色名字：{{ char_name }}
{% endif %}这一栏应该写：{{ field_guidance }}
{% if context_blocks %}
=== 这张卡已经写好的其他栏目（作为参考，不要改写它们）===
{% for block in context_blocks %}
【{{ block.label }}】
{{ block.content }}
{% endfor %}
{% endif %}
{% if is_generate %}
任务：从零写出「{{ field_label }}」这一栏的内容。
{% if context_blocks %}要和上面已有的栏目对得上，不要冒出互相矛盾的设定。{% endif %}
{% else %}
当前「{{ field_label }}」内容：
{{ content }}

任务：保留原有的核心设定，把「{{ field_label }}」写得更具体、更立得住。
可以补充细节、理顺逻辑、删掉空洞的形容词，但不要换掉这个角色的身份和基本取向。
{% endif %}
硬性要求：
- 只输出「{{ field_label }}」这一栏的正文，不要标题、不要前缀、不要任何说明或点评。
- 用陈述性的中文写设定，不要写成对话，不要替这个角色说台词。
- 提到玩家时一律写成 {{ '{{user}}' }}，不要写"你"，也不要自己编一个玩家名字。
  提到这个角色本人时可以写 {{ '{{char}}' }} 或直接写他的名字。这两个占位符会在实际游玩时替换成真名。
- 总长度控制在 {{ max_chars }} 字以内。
