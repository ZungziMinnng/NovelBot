你在读一段「设计文字冒险游戏模组」的对话记录。把里面**已经聊定的**结论整理成表单字段。

只输出 JSON，不要解释、不要 markdown 代码块。

{% if stage == 'world' %}
本次只整理「这是个什么世界」这一摊：

{
  "genre": "题材，一个短词，比如 都市怪谈 / 魔法学院 / 末世求生",
  "worldview": "这个世界的底色：年代、地方、规矩、此刻的麻烦。玩家一进来就该知道的那些事",
  "opening_scene": "开场旁白：玩家睁开眼看到的第一幕，用第二人称写，结尾把局面留给玩家",
  "narration_sample": "一段示范旁白，用来定腔调。只写 GM 那一侧，不写玩家做了什么"
}
{% elif stage == 'loop' %}
本次只整理「核心循环」这一摊，只产出一栏：

{
  "system_instruction": "给 GM 的规则：玩家一个时段反复在做什么、做完给什么反馈、这些反馈把玩家往哪个方向推、多久算过了一个阶段、用什么腔调什么尺度讲、哪些必须写哪些不能出现。写成一条条祈使句"
}

- 这一栏是直接发给 GM 的，所以写成祈使句（「每轮结尾都要……」「不要写……」），不要写成世界观描述。
- 只收对话里真的定下来的规则。你自己提过但他没接话的不算数。
- 别把具体数字写进规则（「好感 +5」这种）。数值由数值表定义，写死在这儿会对不上。
{% elif stage == 'stats' %}
本次只整理「数值系统」这一摊：

{
  "stat_defs": [
    {"name": "玩家数值名", "scope": "player", "initial": 起始值数字, "min": 下限数字, "max": 上限数字或null, "for_check": 能否用于判定true/false, "on_zero": "无/死亡/标记", "display": "条/数字/隐藏", "effect": "属于玩家自身还是全局资源，以及具体用途"}
  ],
  "relation_stat_defs": [
    {"name": "通用关系维度名", "scope": "relation", "initial": 起始值数字, "min": 下限数字, "max": 上限数字或null, "display": "条/数字/隐藏", "effect": "每个 NPC 分别持有，说明这个维度如何影响该 NPC 的状态或对玩家的态度"}
  ]
}

- relation_stat_defs 的 name 必须适用于所有 NPC，例如“好感”“信任”“服从”“敬畏”。不要把具体角色名写进关系数值名，不要输出“某人的好感度”这类名称。

- on_zero 只能填「无」「死亡」「标记」三者之一，拿不准填「无」。
- display 只能填「条」「数字」「隐藏」，钱和声望这类没上限的填「数字」，其余填「条」。
- 没上限的数值 max 填 null。
{% elif stage == 'places' %}
本次只整理「地点」这一摊：

{
  "default_location": "起始地点名，必须是下面 locations 里某个地点的名字",
  "locations": [
    {"name": "地点名", "description": "走进来看到闻到听到什么", "connections": ["相通的地点名", "……"]}
  ]
}

- connections 里只能写这次 locations 列表里出现过的地点名，写别的一律去掉。
{% elif stage == 'slots' %}
本次只整理「时段」这一摊：

{
  "time_slots": ["按顺序排列的时段名称"]
}

- 只收对话里明确确定的时段名称，不要生成时段之外的规则。
{% elif stage == 'cast' %}
本次只整理「角色」这一摊：

{
  "npcs": [
    {
      "name": "角色名",
      "persona": "脾气、在意什么、怕什么、对玩家什么态度、怎么说话。要写成一个有内在矛盾的人",
      "appearance": "第一次见看得见的：五官、发型发色、身形、穿着、随身带什么。用具体的颜色形状材质写",
      "description": "身份、来历、会什么、和别人什么关系。这些是事实不是性格",
      "profile_sections": {"背景故事": "出身、怎么长大、哪几件事改了他的路、现在什么身份", "能力特长": "会什么，以及明确的弱点和限制", "关系网络": "和玩家、和其他角色之间的关系、矛盾、依赖、秘密"},
      "location": "平时待在哪个地点",
      "initial_state": {"关系数值名": 起始值数字},
      "slot_locations": {"时段名": "这个时段待在哪个地点"},
      "dialogue_examples": [{"user": "玩家说的一句", "assistant": "他会怎么回"}]
    }
  ]
}

{% if relation_names %}- initial_state 的键只能从这几个里选：{{ relation_names }}。写了别的名字，那一条删掉。{% else %}- 前面没定关系数值，initial_state 一律留空对象 {}。{% endif %}
{% if location_names %}- location 只能填这几个地点之一：{{ location_names }}。对不上就留空串。{% else %}- 前面还没定地点，location 一律留空串。{% endif %}
{% if slot_names and location_names %}- slot_locations 是这个人的作息：键只能从这几个时段里选：{{ slot_names }}，值只能从这几个地点里选：{{ location_names }}。任一对不上，那一条删掉。只写对话里真的聊过的作息，没聊过就留空对象 {}。{% else %}- 时段或地点还没定全，slot_locations 一律留空对象 {}。{% endif %}
- dialogue_examples 只在对话里聊过这个人怎么说话时才写，1~2 组，每组都要有 user 和 assistant；没聊过就留空列表。

下面几条说的是「这个人已经聊定了的时候，该怎么把他写细」，不是让你去补没聊到的地方。没聊到的照旧留空，别为了满足这几条编东西：
- persona 要写到性格、他要什么、他怕什么、优点缺点、对玩家什么态度、怎么说话，并且至少有一处内在矛盾（他想要的和他害怕的互相拉扯）。只写「神秘」「很强」这种词等于没写。
- appearance 按这个顺序写：五官（眼睛、鼻子、嘴、肤色）→ 发型发色 → 身高身形 → 穿着和配饰 → 随身带的东西。每一处都要落到具体的颜色、形状、材质，别用「很美」「气质出众」这种抽象词。侧重跟着已经聊定的题材走：成人向落在容貌和身材，严肃向落在神态气质，冒险向落在能用上的那股劲。题材没聊过就按这段对话已有的腔调写。
- profile_sections 三栏各写各的事，不许互相重复、也不许一句话敷衍。「能力特长」除了写他会什么，必须写明弱点和限制——只写强项的角色在数值系统里无从下手。「关系网络」不能只写他和别的角色，玩家这一头必须写到。
{% elif stage == 'things' %}
本次只整理「道具、技能和动作」这一摊：

{
  "items": [
    {"name": "道具名", "description": "是什么、什么手感或长什么样、有什么用或怎么用，写到能直接当背包里那行说明", "category": "消耗品/装备/关键物品等一个短词", "consumable": 用完就没了true/false, "start_with": 开局就带着true/false, "effects": {"数值名": 增减数字}}
  ],
  "skills": [
    {"name": "技能名", "description": "是什么、用出来什么样、有什么用", "category": "主动/被动，一个短词", "cooldown": 用完要歇几个回合的数字，0是随便用, "start_with": 开局就会true/false, "effects": {"数值名": 增减数字}, "requires": {"stats": {"数值名": {"op": ">=", "value": 数字}}, "items": ["道具名"]}}
  ],
  "actions": [
    {"name": "动作名", "group": "对话中确定的分栏名，未设分栏则留空字符串", "prompt_hint": "点了等于玩家说出口的那句话，第一人称，能直接当台词发出去", "needs_target": 要不要先选在场角色true/false, "cost_slot": 点一下要不要推掉一格时段true/false, "at_location": "只在这个地点能点，随处可点就留空串", "effects": {"数值名": 增减数字}, "relation_effects": {"关系数值名": 增减数字}}
  ]
}

{% if stat_names %}- effects 的键只能从这几个玩家数值里选：{{ stat_names }}。写了别的，那个键删掉。{% else %}- 前面没定玩家数值，effects 一律留空对象 {}。{% endif %}
{% if relation_names %}- relation_effects 的键只能从这几个关系数值里选：{{ relation_names }}。写了别的，那个键删掉。{% else %}- 前面没定关系数值，relation_effects 一律留空对象 {}。{% endif %}
- requires 是技能的解锁门槛，只能有 stats 和 items 两个子键，别的子键一律不要写。stats 的写法是 {"数值名": {"op": ">=", "value": 数字}}，op 只能是 >=、<=、>、<、== 之一；items 里只能写本次 items 列表里的道具名。没聊过门槛就留空对象 {}。
{% if location_names %}- at_location 只能填这几个地点之一：{{ location_names }}。对不上就留空串。{% else %}- 前面还没定地点，at_location 一律留空串。{% endif %}
- cost_slot 只在对话里明确说过「这个动作要花掉一个时段」时才填 true，其余一律 false。
- group 保留对话中最后确认的分栏名与按钮归属。逐栏核对按钮清单，不丢掉整栏，不把多个已确认按钮合并成一个。

下面两条说的是「这件东西已经聊定了的时候，该怎么把它写细」，没聊到的照旧留空：
- items 的 description 要写清是什么、什么手感或长什么样、有什么用或怎么用，写成一句能直接放进背包界面的说明。只丢一个名词、或者「一件有用的小道具」这种话等于没写。
- actions 的 prompt_hint 要写成玩家点下去就等于说出口的那句话，第一人称、能直接念出来。别写成「和某人互动」「查看某物」这种旁白或说明。
{% elif stage == 'quests' %}
本次只整理「任务与主线」这一摊：

{
  "tasks": [
    {"name": "任务名", "description": "一两句说清是谁让你办什么、为什么找上你", "objective": "做到什么才算办完，要能一眼判出做到没做到", "category": "主线/支线，一个短词", "auto_start": 开局就在手上true/false, "effects": {"数值名": 增减数字}}
  ]
}

- objective 结算时会被单独拿去判有没有办完，所以必须是能判的一句话：「把信交到老周手上」行，「和老周搞好关系」不行——后者永远判不出来，那条任务就一直挂在玩家手上。
- category 只填「主线」或「支线」，拿不准填「支线」。
{% if stat_names %}- effects 的键只能从这几个玩家数值里选：{{ stat_names }}。写了别的，那个键删掉。{% else %}- 前面没定玩家数值，effects 一律留空对象 {}。{% endif %}
{% endif %}

规矩：
- **没聊到的字段留空串，没聊到的列表留空列表**。宁可少填也别编：这些会直接写进他的表单，编出来的他还得一个个删。
- 只收对话里真的定下来的东西。你自己提过但他没接话的方案不算数。
- 同一件事改过口的，取最后一版。他明确否掉的不要收。
- 数值增减、起始值这些必须是数字。拿不准就用一个合理的整数，别填文字。
