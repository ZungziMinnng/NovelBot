以 JSON 格式输出，格式为：
{
  "entities": {
    "实体名": {
      "owner": "当前持有者/归属者",
      "level": "当前等级/层次/阶段",
      "recent_changes": "本章该实体发生的变化（新获得的能力、升级、损坏、消耗、封印、解锁、易主等）"
    }
  },
  "locations": {
    "地点名": {
      "current_situation": "当前局势或状态",
      "control": "当前控制方/所属势力",
      "notable_changes": ["本章造成的地点变化"]
    }
  }
}

注意：
- 只输出本章有变化的实体和地点；某个字段无变化就省略该字段，不要输出空字符串或空数组占位
- 不要重写实体的整体描述或罗列全部已知能力，只记录本章发生的增量变化
- 某个部分本章没有任何变化，就输出空对象 {}
只输出 JSON，不要任何解释。
