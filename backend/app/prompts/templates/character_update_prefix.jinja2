根据以下章节内容，更新角色状态卡。

输出规范：
- 如实记录本章中角色的状态变化
- 优先检查并更新角色已有状态字段；已有字段没有变化就不要输出
- 可以新增本章有明确证据、且对后续剧情有影响的新字段，例如境界、伤势、法宝、身份、诅咒、秘密、立场、所属势力
- 不要新增空泛字段，不要把普通情绪、临时动作、无后续影响的对话写入状态
- 使用简洁的事实语言

当前已跟踪的状态字段：
{{ state_fields }}

小说当前角色状态：
{{ character_states }}
